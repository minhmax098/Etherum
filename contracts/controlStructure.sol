pragma solidity 0.8;

contract ControlStructure {
    // controlStructure: câu lệnh vòng lặp, if-else
    // uint public value;
    bool public IsTrue = true;
    
    function DoSomething() external { // parameter
        //if (IsTrue == true && msg.sender == "") { // operator

        // }
        //else {
           //
        //}
        // for (uint i=0; i < 10; i++)
        //{
            // i=0, 1...9
            // do something
        //}
        bool isOk = true;
        while (isOk){ 
            // do something
            //if() {
                //isOk = false;
            //}
            // continue 
            // break
        }
    }
}